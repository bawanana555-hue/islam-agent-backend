name = "islam-agent"
type = "javascript"
account_id = "VOTRE_ACCOUNT_ID"  # À remplacer par votre Account ID Cloudflare
workers_dev = true
route = ""
zone_id = ""

# Variables d'environnement (si besoin)
[vars]
ENVIRONMENT = "production"
WORKER_NAME = "islam-agent-epistemique"

# Comportement du Worker
[env.production]
vars = { ENVIRONMENT = "production" }

# Durable Objects si besoin (pas utilisé pour l'instant)
# [durable_objects]
# bindings = []

# R2 Bucket pour stockage (optionnel)
# [r2_buckets]
# binding = "STORAGE"
